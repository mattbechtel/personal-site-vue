import { defineComponent } from 'vue';

export default defineComponent({
    name: 'Resume',
    props: {
        msg: String,
    },
    data(): void {
        return {};
    },
    methods: {},
});
